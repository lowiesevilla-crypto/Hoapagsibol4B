export function PageHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description?: string; action?: React.ReactNode }) {
  return <header className="relative mb-7 overflow-hidden rounded-3xl border border-pine-100 bg-white/90 px-4 py-5 shadow-soft backdrop-blur-sm sm:px-6"><span className="pointer-events-none absolute inset-y-0 left-0 w-1.5 bg-gradient-to-b from-leaf-500 to-pine-500" /><span className="pointer-events-none absolute -right-12 -top-16 size-36 rounded-full bg-leaf-100/50" /><div className="relative flex min-w-0 flex-col justify-between gap-4 sm:flex-row sm:items-end"><div className="min-w-0">{eyebrow && <p className="mb-1.5 break-words text-xs font-extrabold uppercase tracking-[.16em] text-pine-600 sm:tracking-[.2em]">{eyebrow}</p>}<h1 className="break-words text-2xl font-black tracking-tight text-ink sm:text-3xl">{title}</h1>{description && <p className="mt-1.5 max-w-2xl text-sm leading-6 text-slate-500">{description}</p>}</div>{action && <div className="flex w-full flex-wrap gap-2 sm:w-auto sm:justify-end">{action}</div>}</div></header>;
}
