name: HOAHub Table Standard

on:
  pull_request:
    branches: [main, develop]

jobs:
  table-standard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inventory and verify repository tables
        run: node scripts/verify-standard-tables.mjs
